import type { Summary } from "../../lib/domain";

const gen = (amount: bigint) => `${new Intl.NumberFormat("en-US").format(amount)} GEN`;
export function ImpactHero({ summary, grantCount }: { summary?: Summary; grantCount: number }) { const funded = summary?.grantInflows ?? 0n; const paid = summary?.completedPayouts ?? 0n; return <section className="impact-hero" aria-labelledby="hero-title"><div className="impact-hero__copy"><p className="eyebrow">Community grants, on the trail</p><h1 id="hero-title">Where good ideas grow <em>visible roots.</em></h1><p className="hero-intro">Support locally-led work with public milestones, evidence, and transparent simulated GEN escrow.</p><div className="hero-actions"><a className="button button--coral" href="#explore-grants">Explore projects <span aria-hidden="true">↓</span></a><a className="text-link" href="#how-it-works">See how it works <span aria-hidden="true">→</span></a></div></div><div className="hero-art" role="img" aria-label="Volunteers tending a community garden"><div className="hero-orbit hero-orbit--one" /><div className="hero-orbit hero-orbit--two" /><div className="hero-stamp"><span>Proof-led</span><strong>impact</strong><span>since today</span></div></div><dl className="hero-metrics"><div><dt>Projects on trail</dt><dd>{grantCount}</dd></div><div><dt>GEN committed</dt><dd>{gen(funded)}</dd></div><div><dt>GEN paid after proof</dt><dd>{gen(paid)}</dd></div><div><dt>Network</dt><dd className="metric-network"><i />Studionet</dd></div></dl></section>; }
